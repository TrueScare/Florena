<form method="post" action="{{ path('app_propagation_actions_delete', {'id': propagation_action.id}) }}" onsubmit="return confirm('Bist du dir sicher, dass du die Aufgabe löschen möchtest?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ propagation_action.id) }}">
    <button class="btn">Löschen</button>
</form>
